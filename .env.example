# ─────────────────────────────────────────────────────────────────────────────
# Kalakosh Zurich — Environment Variables
# Copy this file to .env and fill in all values before running docker compose.
# ─────────────────────────────────────────────────────────────────────────────

# ── Database ──────────────────────────────────────────────────────────────────────────
 MYSQL_ROOT_PASSWORD=change_me_root_password
MYSQL_DATABASE=kalakosh
MYSQL_USER=kalakosh_user
MYSQL_PASSWORD=change_me_db_password

# ── Session ───────────────────────────────────────────────────────────────────────────
# Generate with: openssl rand -hex 32
JWT_SECRET=a_long_random_string_at_least_32_chars

# ── Google OAuth ──────────────────────────────────────────────────────────────────
# 1. Go to https://console.cloud.google.com → APIs & Services → Credentials
# 2. Create an OAuth 2.0 Client ID (Web application)
# 3. Add Authorized redirect URI: https://yourdomain.com/api/oauth/callback
# 4. Copy Client ID and Client Secret below
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret

# The only Google account allowed to log in as admin
ADMIN_EMAIL=shwena9@gmail.com

# ── Discord Bot ────────────────────────────────────────────────────────────────────
# Bot token from discord.com/developers/applications → Bot → Token
DISCORD_BOT_TOKEN=Bot_token_here

# Channel ID where you post new products (right-click channel → Copy Channel ID)
DISCORD_CHANNEL_ID=123456789012345678

# Your personal Discord user ID for receiving "product added" DM notifications
# Right-click your own name in Discord → Copy User ID (Developer Mode must be on)
# Optional — if not set, notifications are logged to the server console instead
DISCORD_OWNER_USER_ID=

# ── Stripe Payments ───────────────────────────────────────────────────────────────
# Online checkout supports credit cards, debit cards and TWINT (all in CHF).
# 1. Create a Stripe account (use a Swiss/CHF account so TWINT is available).
# 2. Dashboard → Developers → API keys → copy the Secret key below.
# 3. Dashboard → Payments → enable "TWINT" and "Cards" payment methods.
# 4. Dashboard → Developers → Webhooks → add endpoint
#    https://yourdomain.com/api/stripe/webhook and subscribe to
#    checkout.session.completed, checkout.session.async_payment_succeeded,
#    checkout.session.async_payment_failed, checkout.session.expired
#    then copy the "Signing secret" into STRIPE_WEBHOOK_SECRET.
# If STRIPE_SECRET_KEY is left blank, checkout falls back to WhatsApp enquiry.
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_signing_secret

# ── Stripe Test Keys (for integration tests) ──────────────────────────────────────
# These are used by `pnpm test:integration` to run real API tests against
# Stripe's test environment. They should match your STRIPE_SECRET_KEY and
# STRIPE_WEBHOOK_SECRET above if you're using test keys. In CI, set these
# as GitHub repository secrets (Settings → Secrets → Actions).
# Integration tests auto-skip if these are unset.
STRIPE_TEST_SECRET_KEY=
STRIPE_TEST_WEBHOOK_SECRET=

# Canonical site URL used for Stripe success/cancel redirects.
# Leave blank to derive it from the incoming request origin.
PUBLIC_BASE_URL=https://yourdomain.com

# ── Docker Compose: Caddy (docker-compose.yml) ─────────────────────────────────
# Domain Caddy requests a Let's Encrypt cert for and reverse-proxies to the app.
SITE_DOMAIN=yourdomain.com
# Host ports Caddy binds. Defaults deliberately avoid 80/443 so this stack can
# run alongside another instance (e.g. Kalakosh-ch) on the same server without
# a port clash — put a host-level reverse proxy in front if you need this one
# reachable on the standard ports too.
HTTP_PORT=8080
HTTPS_PORT=8443

# ── POS Terminal (Tap to Pay for market stall sales) ───────────────────────────
# The POS Android and iOS apps authenticate every request with this key via the
# X-POS-Key header. Generate with: openssl rand -hex 32
POS_API_KEY=
# Dashboard → Developers → Webhooks → add endpoint
#   https://yourdomain.com/api/pos/webhook and subscribe to payment_intent.succeeded
#   then copy the "Signing secret" here.
STRIPE_POS_WEBHOOK_SECRET=
# Stripe Terminal Location for Tap to Pay. Served to the POS apps (Android + iOS)
# via GET /api/pos/config so no Stripe config is baked into the app builds.
#   Dashboard → More → Terminal → Locations → create one → copy its id (tml_…).
# Leave blank until set up; the apps then show a "card payments not set up yet"
# screen with the remaining steps instead of failing.
STRIPE_LOCATION_ID=

# ── Transactional Email (Resend) ─────────────────────────────────────────────────────
# Sends order receipt emails to customers after a successful payment.
# 1. Sign up at https://resend.com and verify your domain (kalakosh.ch).
# 2. Create an API key and paste it below.
# 3. Set RESEND_FROM_EMAIL to a verified sender address on your domain.
# If RESEND_API_KEY is left blank, receipt emails are silently skipped.
RESEND_API_KEY=re_your_resend_api_key
RESEND_FROM_EMAIL=Kalakosh <orders@kalakosh.ch>

# ── LLM — OpenAI-compatible API ───────────────────────────────────────────────────────
# Option A: Groq (fast, generous free tier — recommended)
# LLM_BASE_URL=https://api.groq.com/openai/v1
# LLM_API_KEY=gsk_...
# LLM_MODEL=llama3-8b-8192

# Option B: OpenAI
# LLM_BASE_URL=https://api.openai.com/v1
# LLM_API_KEY=sk-...
# LLM_MODEL=gpt-4o-mini

# Option C: Ollama (fully local, free — must be accessible from the container)
# LLM_BASE_URL=http://host.docker.internal:11434/v1
# LLM_API_KEY=ollama
# LLM_MODEL=llama3

LLM_BASE_URL=https://api.groq.com/openai/v1
LLM_API_KEY=your_groq_api_key
LLM_MODEL=llama3-8b-8192

# ── S3-compatible Storage ──────────────────────────────────────────────────────────────
# Option A: Cloudflare R2 (recommended — free 10 GB, no egress fees)
# S3_ENDPOINT=https://<account_id>.r2.cloudflarestorage.com
# S3_REGION=auto
# S3_PUBLIC_URL=https://pub-<hash>.r2.dev   ← enable public access in R2 dashboard

# Option B: AWS S3
# S3_ENDPOINT=   ← leave blank for AWS
# S3_REGION=eu-central-1
# S3_PUBLIC_URL=  ← leave blank to use signed URL proxy

# Option C: Backblaze B2
# S3_ENDPOINT=https://s3.us-west-004.backblazeb2.com
# S3_REGION=us-west-004
# S3_PUBLIC_URL=https://f004.backblazeb2.com/file/<bucket>

S3_BUCKET=kalakosh-images
S3_REGION=auto
S3_ACCESS_KEY_ID=your_access_key
S3_SECRET_ACCESS_KEY=your_secret_key
S3_ENDPOINT=https://your_account_id.r2.cloudflarestorage.com
# Leave blank to serve via signed URL proxy (/uploads/* route)
S3_PUBLIC_URL=

# ── Backup: secondary S3 (redundancy) ────────────────────────────────────────
# Point at a DIFFERENT provider from the primary above (e.g. Backblaze B2 if
# primary is Cloudflare R2). deploy/backup.sh uploads to both — if one goes
# down the other still has your backup. Leave blank to skip.
BACKUP_S3_BUCKET=kalakosh-backups
BACKUP_S3_REGION=us-west-004
BACKUP_S3_ACCESS_KEY_ID=your_b2_key_id
BACKUP_S3_SECRET_ACCESS_KEY=your_b2_app_key
BACKUP_S3_ENDPOINT=https://s3.us-west-004.backblazeb2.com

# ── Backup: GitHub private repository ────────────────────────────────────────
# Each weekly backup is committed as backup.sql + inventory.csv to this repo.
# Git history lets you diff any two weeks to see exactly what stock changed.
# Setup:
#   1. Create a private repo (e.g. youruser/kalakosh-backups)
#   2. Generate a fine-grained PAT: Settings → Developer settings →
#      Fine-grained tokens → Contents: Read and Write for that repo only
# Leave blank to skip.
BACKUP_GITHUB_REPO=youruser/kalakosh-backups
BACKUP_GITHUB_TOKEN=github_pat_...

# ── CI/CD Secret Sync (deploy/rotate-pos-key.sh) ─────────────────────────────
# Used by deploy/rotate-pos-key.sh to push a newly generated POS_API_KEY to
# CircleCI and Codemagic automatically. Without these, the script skips those
# platforms and you must update them manually in their dashboards.

# CircleCI personal API token.
# Get from: circleci.com → User Settings (top-right avatar) → Personal API Tokens → Create New Token
CIRCLECI_TOKEN=

# Codemagic personal API token.
# Get from: codemagic.io → Teams → Personal Account → Integrations → Codemagic API → Show
CODEMAGIC_TOKEN=

# Codemagic application ID for the kalakosh-pos-ios app.
# Get from the URL when viewing the app in Codemagic: codemagic.io/app/<APP_ID>/settings
CODEMAGIC_APP_ID=
