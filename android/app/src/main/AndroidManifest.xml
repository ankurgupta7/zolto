<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.NFC" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- Required by Stripe Terminal Tap to Pay reader discovery/connection. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- Required for offline sync scheduling -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".PosApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PosApp">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            exported=true because of the pairing deep link below: tapping
            `gwinn://pair?t=…` in the store admin hands this activity the token
            and the register binds itself, instead of a merchant typing 64 hex
            characters into a phone.

            A custom scheme rather than an https App Link, because verified App
            Links need an assetlinks.json carrying the signing certificate
            fingerprint — which a debug build published from CI does not have a
            stable one of. The token in the link is single-use and short-lived,
            and is exchanged for the real key server-side, so another app
            registering the same scheme intercepts at most one spent token and
            never the POS key itself.
        -->
        <activity
            android:name=".SetupActivity"
            android:exported="true">
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="gwinn" android:host="pair" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SaleReviewActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".PaymentActivity"
            android:exported="false"
            android:parentActivityName=".SaleReviewActivity" />

        <activity
            android:name=".SuccessActivity"
            android:exported="false" />

        <activity
            android:name=".SalesHistoryActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

    </application>

</manifest>
